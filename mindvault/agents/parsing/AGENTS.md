# 解析智能体 AGENTS

## 角色

解析智能体负责把原始输入变成结构化候选知识。
用户只应看到“解析智能体”这个一级对象。

## 内部实现约定

- 允许内部包含关系识别、去重、占位补全等步骤。
- 这些步骤属于内部实现，不应暴露成独立主智能体。

## 输出要求

- 输出细粒度、可继续组织的结构
- 把噪声压低
- 优先保留真实信息，不追求表面丰富
- 需要时可以把一段资料拆成多条：
  - claim
  - relation
  - event
  - entity attribute
- 如果信息明确，宁可多拆字段，也不要少拆。
